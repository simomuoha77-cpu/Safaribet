services:
  - type: web
    name: betake-platform
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: ODDS_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
